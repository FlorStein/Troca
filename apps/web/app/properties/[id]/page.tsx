import {Header,Footer,Btn} from '@/components/ui';import {properties} from '@/lib/data';
export default function PropertyDetail({params}:{params:{id:string}}){const p=properties.find(x=>x.id===params.id)||properties[0];return <><Header app/><main className="wrap appmain detail"><div className="gallery"><img src={p.image} alt={p.name}/><h1>{p.name}</h1><p className="location">⌖ {p.city}</p><p>Una casa cuidada por miembros de la comunidad, pensada para disfrutar el destino con comodidad y calma.</p></div><aside className="panel"><span className="badge">Propiedad verificada</span><h2>{p.points} puntos / noche</h2><p>Capacidad para {p.guests} huéspedes · 2 habitaciones · 1 baño</p><p>Wifi · Cocina · Jardín · Estacionamiento</p><Btn href="/exchanges">Solicitar intercambio</Btn></aside></main><Footer/></>}
export function generateStaticParams(){return properties.map(({id})=>({id}));}
