import {Header,Footer,Btn} from '@/components/ui';import {hotels} from '@/lib/data';
export default function HotelDetail({params}:{params:{id:string}}){const h=hotels.find(x=>x.id===params.id)||hotels[0];return <><Header app/><main className="wrap appmain detail"><div className="gallery"><img src={h.image} alt={h.name}/><h1>{h.name}</h1><p className="location">⌖ {h.city} · ★ {h.rating}</p><p>Un hotel aliado seleccionado por su hospitalidad, ubicación y experiencia local.</p></div><aside className="panel"><span className="badge">Beneficio Troca</span><h2>{h.benefit}</h2><p><del>Precio rack: USD 170</del><br/>Precio para miembros desde USD 120.</p><p>Disponibilidad: Abril · Mayo · Junio · Agosto</p><Btn href="/membership">Reservar con mi membresía</Btn></aside></main><Footer/></>}
export function generateStaticParams(){return hotels.map(({id})=>({id}));}
